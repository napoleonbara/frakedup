<%= form_for(@card_model) do |f| %>
  <% if @card_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_model.errors.count, "error") %> prohibited this card_model from being saved:</h2>

      <ul>
      <% @card_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
